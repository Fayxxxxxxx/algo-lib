好的大王，一个一个问题来。

  ---
  第一：大数 ÷ int 的竖式套路

  你写 1234 ÷ 5，小学竖式是这样的：

          0 2 4 6
        ─────────
     5 ) 1 2 3 4
         0         ←0×5
         ─
         1 2       ←余1，拖下2
         1 0       ← 2×5
         ──
           2 3     ←余2，拖下3
           2 0     ←4×5
           ──
             3 4   ←余3，拖下4
             3 0   ←6×5
             ──
               4   ←最终余数

  套路就三步，循环做：

  循环开始，手里拿着一个"余数 r"（初始 r=0）：

  第1步：拖下一位 →r = r×10+ 下一位数字
  第2步：试商     →商 = r ÷除数
  第3步：留余     →r = r % 除数  （余数留给下一轮）

  对着上面走一遍：

  ┌──────┬────────┬──────────────────────┬────────┬────────┐
  │ 轮次 │ 当前位 │ 拖下来 r = r×10+ 位 │ 商 r÷5│ 余 r%5 │
  ├──────┼────────┼──────────────────────┼────────┼────────┤
  │ 1    │ 1      │ 0×10+1= 1           │ 0      │ 1      │
  ├──────┼────────┼──────────────────────┼────────┼────────┤
  │ 2    │ 2      │ 1×10+2= 12          │ 2      │ 2      │
  ├──────┼────────┼──────────────────────┼────────┼────────┤
  │ 3    │ 3      │ 2×10+3= 23          │ 4      │ 3      │
  ├──────┼────────┼──────────────────────┼────────┼────────┤
  │ 4    │ 4      │ 3×10+4= 34          │ 6      │ 4      │
  └──────┴────────┴──────────────────────┴────────┴────────┘

  商的每一位依次是：0 →2 →4 →6，合起来 0246 = 246，余 4。

  代码就是对这三步的翻译：

  vector<int> div(vector<int>& a, int b) {
      vector<int> c;   // 商
      int r = 0;       // 手里拿着的余数，初始为 0

      for (int i = a.size() - 1; i >= 0; i--) {  // 从最高位往最低位扫
          r = r * 10 + a[i];   // 第1步：拖下当前位
          c.push_back(r / b);  // 第2步：试商，记下来
          r = r % b;           // 第3步：留余数，给下一轮
      }
      // 此时 c 是高位在前 [0,2,4,6]，翻转成低位在前 [6,4,2,0]
      reverse(c.begin(), c.end());
      while (c.size() > 1 && c.back() == 0) c.pop_back();  // 去掉前导零
      return c;
  }

  就这三步，没有别的了。 你小学怎么列竖式的，代码就怎么写的。

  ---
  第二：大数 ×大数

  这个也是小学竖式，只是用数组存。

  小学怎么做 123 ×45？

          1 2 3
      ×    4 5
      ─────────
          6 1 5    ←123 ×5
        4 9 2      ←123 ×4，往左错一位
      ─────────
        5 5 3 5

  翻译成套路：

  ▎ a 的第 i 位 ×b 的第 j 位，结果加到 c 的第 i+j 位。

  123 ×45：

  a = [3, 2, 1]  (123，个十百)
  b = [5, 4]     (45，个十)

  a[0]=3 ×b[0]=5 = 15 →c[0+0] = c[0] += 15
  a[1]=2 ×b[0]=5 = 10 →c[1+0] = c[1] += 10
  a[2]=1 ×b[0]=5 = 5  →c[2+0] = c[2] += 5

  a[0]=3 ×b[1]=4 = 12 →c[0+1] = c[1] += 12
  a[1]=2 ×b[1]=4 = 8  →c[1+1] = c[2] += 8
  a[2]=1 ×b[1]=4 = 4  →c[2+1] = c[3] += 4

  c = [15, 22, 13, 4]
  然后统一处理进位：
  c[0]=15 →留5进1
  c[1]=22+1=23 →留3进2
  c[2]=13+2=15 →留5进1
  c[3]=4+1=5 →留5

  结果 c = [5, 3, 5, 5] →5535 ✓

  代码：

  vector<int> mul_big(vector<int>& a, vector<int>& b) {
      vector<int> c(a.size() + b.size());  // 结果最多 a位数+b位数

      // 第1步：逐位相乘，加到对应位置
      for (int i = 0; i < a.size(); i++) {
          for (int j = 0; j < b.size(); j++) {
              c[i + j] += a[i] * b[j];
          }
      }

      // 第2步：统一处理进位
      int t = 0;
      for (int i = 0; i < c.size(); i++) {
          t += c[i];
          c[i] = t % 10;
          t /= 10;
      }

      // 第3步：去前导零
      while (c.size() > 1 && c.back() == 0) c.pop_back();
      return c;
  }

  ---
  完整模板

  把乘法和除法放一起，你存好：

  #include<bits/stdc++.h>
  using namespace std;

  // 去前导零
  void trim(vector<int>& a) {
      while (a.size() > 1 && a.back() == 0) a.pop_back();
  }

  // 大数 ×int
  vector<int> mul_int(vector<int>& a, int b) {
      vector<int> c;
      int t = 0;
      for (int i = 0; i < a.size() || t; i++) {
          if (i < a.size()) t += a[i] * b;
          c.push_back(t % 10);
          t /= 10;
      }
      return c;
  }

  // 大数 ×大数
  vector<int> mul_big(vector<int>& a, vector<int>& b) {
      vector<int> c(a.size() + b.size());
      for (int i = 0; i < a.size(); i++)
          for (int j = 0; j < b.size(); j++)
              c[i + j] += a[i] * b[j];
      int t = 0;
      for (int i = 0; i < c.size(); i++) {
          t += c[i];
          c[i] = t % 10;
          t /= 10;
      }
      trim(c);
      return c;
  }

  // 大数 ÷int
  vector<int> div_int(vector<int>& a, int b) {
      vector<int> c;
      int r = 0;
      for (int i = a.size() - 1; i >= 0; i--) {
          r = r * 10 + a[i];    // 拖下一位
          c.push_back(r / b);   // 试商
          r %= b;               // 留余
      }
      reverse(c.begin(), c.end());
      trim(c);
      return c;
  }

  // 打印大数（低位在前）
  void print(vector<int>& a) {
      for (int i = a.size() - 1; i >= 0; i--) cout << a[i];
  }